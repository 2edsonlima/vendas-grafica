version: '3.8'

services:
  backend:
    build: ./backend
    container_name: catalogo_backend
    ports:
      - "3001:3001"

  frontend:
    build: ./frontend
    container_name: catalogo_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend